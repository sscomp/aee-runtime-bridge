"""Research framework for the Task Dispatcher.

This package provides:
- scheduler: detect / list scheduled jobs (cron, supervisord, systemd)
- repo: scan a repository (files, deps, import graph, entry points)
- prompts: load prompt templates by version
- report: 9-section unified report scaffolding
"""
from __future__ import annotations
